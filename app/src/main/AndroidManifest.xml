<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahasiswa.mylogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
                  android:theme="@style/SecondaryTheme"
                  android:label="@string/demiholoh">
        </activity>
        <activity android:name=".Main2Activity"
                  android:theme="@style/ThirdTheme"
                  android:label="@string/demiholoh">
        </activity>
        <activity android:name=".hal2"
                  android:theme="@style/ForthTheme"
                  android:label="@string/demiholoh">
        </activity>
        <activity android:name=".hal3"
                  android:theme="@style/FifthTheme"
                  android:label="@string/demiholoh">
        </activity>
        <activity android:name=".hal4"></activity>

    </application>

</manifest>